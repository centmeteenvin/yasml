name: yasml
description: "Yet Another State Management Library — explicit, traceable, compiler-verified state management for Flutter."
version: 0.3.0
resolution: workspace
homepage: https://github.com/centmeteenvin/yasml
repository: https://github.com/centmeteenvin/yasml
issue_tracker: https://github.com/centmeteenvin/yasml/issues

topics:
  - state-management
  - flutter
  - reactive

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

environment:
  sdk: ^3.7.0
  flutter: ">=3.35.0"

dependencies:
  async: ^2.13.0
  collection: ^1.19.1
  flutter:
    sdk: flutter
  logging: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^10.0.0

flutter:
